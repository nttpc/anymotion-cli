[tox]
envlist = py36, py37, py38, flake8, black, bandit, mypy

[testenv]
deps =
    pytest
    pytest-mock
    requests-mock
commands = pytest {posargs}

[testenv:flake8]
deps =
    flake8
    flake8-docstrings
skip_install = true
commands = flake8

[testenv:black]
deps = black
skip_install = true
commands = black . --check

[testenv:bandit]
deps = bandit
skip_install = true
commands = bandit -r encore_api_cli -s B303

[testenv:mypy]
deps = mypy
skip_install = true
commands = mypy
